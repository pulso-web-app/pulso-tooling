## 1. Aggregate Environment

- [x] 1.1 Add deterministic child-environment normalization and verify unit coverage for color-variable conflicts
- [x] 1.2 Apply normalization to parallel and sequential aggregate commands and verify exit-code behavior remains covered

## 2. Cross-Repository Validation

- [x] 2.1 Run `npm run test:apps` and verify known Node, builder, and CRM runtime warnings are absent
- [x] 2.2 Run strict OpenSpec validation and the tooling quality gate
