# Copilot Instructions

Follow the repository's root `AGENTS.md` and the nearest scoped `AGENTS.md` for the files being changed. Those files are authoritative; do not duplicate or override their rules here.
