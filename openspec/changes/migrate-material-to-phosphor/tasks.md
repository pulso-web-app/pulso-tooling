## 1. Repository Coordination

- [x] 1.1 Confirm `pulso-shell` and `pulso-crm` use the shared change ID and `pulso-projects` remains unchanged by comparing repository OpenSpec and source inventories.
- [x] 1.2 Confirm affected repositories retain Angular Material and CDK dependencies and components while adding Phosphor by reviewing package and source diffs.

## 2. Integrated Verification

- [x] 2.1 Run `npm run check` in Tooling, Shell, and CRM and record successful repository-local validation.
- [x] 2.2 Start Shell, CRM, and Projects together and visually verify login, contact list, contact dialog, navigation, buttons, fields, selects, menus, pagination, and date/time controls at desktop and narrow viewports.
